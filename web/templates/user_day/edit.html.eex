<h2>Edit user day</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_day_path(@conn, :update, @user_day) %>

<%= link "Back", to: user_day_path(@conn, :index) %>
